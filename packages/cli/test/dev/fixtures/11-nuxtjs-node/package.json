{
  "name": "nuxtjs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt --port $PORT",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "prepare": "yarn run snyk-protect",
    "snyk-protect": "snyk-protect"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.16.0",
    "@snyk/protect": "latest"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "snyk": true
}
